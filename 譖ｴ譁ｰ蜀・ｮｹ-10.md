# 更新パッケージ 10（bosai-update-10）

今回の修正内容と、GitHubへの上げ方をまとめました。
専門用語はできるだけ避けています。上から順にお読みください。

---

## 1. 今回直したこと（6点）

| # | ご依頼 | どう直したか |
|---|--------|--------------|
| 1 | 紙回答の代理入力をしやすい形にアンケート回答用紙を修正したい | 用紙の並び順と選択肢の番号を、代理入力画面と完全にそろえました |
| 2 | 管理画面からのアンケート回答用紙印刷機能（QRコード入り） | 「アンケート用紙の印刷」を新設。右上に調査回のQRコードが入ります |
| 3 | 回答者の結果画面に回答者平均値を表示させない | 結果画面・保存画像の両方から地域平均を外しました |
| 4 | 調査回の区分に「その他」を追加 | 区分に「その他」を足しました（SQLを1回流す必要があります） |
| 5 | 回答URLからQRコードの作成と画像形式で保存 | 調査回の一覧に「QRコード」ボタンを追加。PNG画像で保存できます |
| 6 | 総会用印刷資料に余分なページが出てしまう | 原因を直しました。試したところ **19ページ → 2ページ** になりました |

---

## 2. それぞれの中身

### 1）アンケート用紙と代理入力の並びをそろえました

紙を見ながら打ち込むとき、目線が紙と画面を往復するのが一番の負担でした。
そこで、用紙の側を入力画面に合わせています。

- **並び順を同じにしました**
  回答者について → 初動対応力 → 知識チェック → 防災行動力 → その他
  （これは代理入力画面の上から下への並びと同じです）
- **選択肢の□に数字を振りました**
  用紙の「□1 参加している」の **1** が、そのまま入力時に押す数字キーです。
  知識チェックは 1＝〇、2＝×。
  用紙を左手に持ち、数字キーだけを叩けば1枚が終わります。
- **№も同じにしました**
  用紙の№と、入力画面の左端に出る番号が一致します。
  途中で見失っても、番号で突き合わせられます。
- **用紙の下に「役員記入欄」を付けました**
  「□入力済／入力者／入力日」。二重入力や入力漏れを防げます。

### 2）アンケート用紙の印刷

管理画面に「**アンケート用紙の印刷**」という箱が増えました
（「紙回答の代理入力」のすぐ上です）。

1. 印刷したい調査回を選ぶ
2. 「用紙を開く」を押す
3. 「印刷／PDFに保存」を押す

A4縦・2枚に収まるよう組んであります。両面印刷にすれば1枚です。
「送信先」を「PDFに保存」にすれば、そのまま印刷所やコンビニ印刷にも使えます。

同梱の **「見本_アンケート用紙.pdf」** で、刷り上がりを先にご覧いただけます。
（見本のQRコードは架空の調査回のものです。実際の用紙にはその調査回のURLが入ります）

### 3）結果画面から地域平均を外しました

ご指摘のとおり、一斉に回答していただく場では

- 先に答えた方には（5名に満たないため）そもそも平均が出ない
- 少人数のうちに出た平均は、実態とかけ離れた値になる

という状態でした。結果画面のレーダーチャートからも、
画像で保存したときの図からも、地域平均を外しています。

代わりに「地域全体の集計結果は、後日自治会からお知らせします」という
一文はそのまま残してあります。

なお、**管理画面の個票（回答者ごとのカード）に出る地域平均はそのままです**。
こちらは全員の回答がそろってから役員がご覧になるものなので、
外す理由がないと判断しました。

### 4）調査回の区分に「その他」を追加

新規作成・編集の両方の「区分」に「その他」が並びます。
調査回の一覧にも、区分が表示されるようにしました。

**この機能だけは、SQLを1回流す必要があります**（下の「4. SQLの実行」）。
流す前に「その他」で保存しようとすると、
「まだデータベースに登録されていません」という案内が出ます。

### 5）QRコードの作成と画像保存

「調査回の管理」の一覧に「**QRコード**」ボタンが増えました。
押すと、その調査回の回答URLのQRコードが表示されます。

- 「QRコードを画像で保存」→ 1200ピクセル前後のPNG画像が保存されます
- 「回答URLをコピー」→ 文字としてのURLをコピーします

保存した画像は、回覧板・掲示板・広報紙・ポスターなどに貼り付けて使えます。
A4に4分の1ほどの大きさで刷ってもつぶれません。

QRコードの計算は端末の中だけで行っています。
外部のQR作成サイトに回答URLを送ることはありません。

### 6）総会用印刷資料の余分なページ

原因は2つ重なっていました。

**原因1：隠した部分の「場所」が残っていた**
一部分だけを印刷するしくみが、印刷しない部分を
「見えなくする（visibility:hidden）」やり方でした。
これは見えなくなるだけで場所は残ります。
そのため、管理画面の長い中身がそのまま紙の高さとして数えられ、
資料のあとに白紙が続いていました。
→ 印刷する部分とその親だけに印を付け、
　それ以外は「場所ごと消す（display:none）」やり方に変えました。

**原因2：紙の幅より広く指定していた**
資料の幅を210mm（A4の幅ちょうど）と決め打ちしていました。
A4に12mmずつ余白を取ると、実際に刷れる幅は186mmです。
はみ出した24mm分を刷るために、横方向にもページが増えていました。
→ 余白を差し引いた幅に自動で合わせるようにしました。

あわせて、改ページを「1枚目の後ろ」ではなく「2枚目の前」に入れるよう変えました。
以前の書き方だと、2枚目が無いときに白紙が1枚増えることがありました。

**確認結果**：管理画面に長い中身がある状態で試したところ、
修正前は19ページ、修正後は2ページになりました。
この修正は、個票（回答者カード）の印刷にも同じように効きます。

---

## 3. GitHubへの上げ方

zipを解凍すると、次のようになっています。

```
package.json
src/
  AdminDashboard.jsx      ← 差し替え
  BosaiSurvey.jsx         ← 差し替え
  ReportSheet.jsx         ← 差し替え
  ResultImage.jsx         ← 差し替え
  SurveyForm.jsx          ← 新規
  QrCode.jsx              ← 新規
  lib/
    bosai-supabase-api.js ← 差し替え
    qr.js                 ← 新規
sql/
  06-phase-other.sql      ← 新規
```

手順は、これまでと同じです。

1. GitHubで `sugi-18/bosai-app` を開く
2. 「Add file」→「Upload files」
3. 解凍してできた **`package.json` `src` `sql` を、点線の枠の中へドラッグ＆ドロップ**
   - ※「choose your files」から選ぶ方法だと、フォルダの形が保てません。
     必ず**ドラッグ＆ドロップ**でお願いします
4. 下の「Commit changes」を押す

同じ名前のファイルは上書きされます。触っていないファイルはそのままです。

押してから2〜3分で、GitHub Actions が自動で公開まで済ませます。
「Actions」タブで緑のチェックが付けば完了です。

**package.json も必ず上げてください。** QRコードを作る部品
（qrcode-generator）を新しく使うため、ここに1行足してあります。
これが無いと、公開の途中で止まります。

---

## 4. SQLの実行（「その他」を使うときだけ）

区分の「その他」を使う場合のみ、1回だけ実行が必要です。
他の5つの修正には、SQLの実行はいりません。

1. Supabase を開く →左の「SQL Editor」
2. `sql/06-phase-other.sql` の中身を全部貼り付ける
3. 「Run」を押す

何度実行しても壊れません。

もし
`ALTER TYPE ... cannot run inside a transaction block`
というエラーが出たときは、ファイルの中の

```sql
alter type round_phase_t add value if not exists 'other';
```

この1行だけを選んで Run し、そのあと残りをまとめて Run してください。

**このファイルには、合言葉の大文字・小文字の扱いをそろえる修正も入れてあります。**
`get_open_round` と `get_round_item_averages` の2つが、
まだ大文字・小文字を区別する古いままでした
（`submit_response` だけが以前に直されていました）。
小文字で入力された合言葉が通らない、という現象の元になりますので、
この機会にそろえています。

---

## 5. 確認していただきたいこと

公開後、次の順にご確認ください。

1. **管理画面** → 「調査回の管理」に「QRコード」ボタンが出るか。
   押して、QRをスマートフォンで読み取り、回答画面が開くか
2. **管理画面** → 「アンケート用紙の印刷」→「用紙を開く」→
   プレビューが2枚に収まっているか。QRが右上に入っているか
3. **管理画面** → 「総会用の印刷資料」→「印刷／PDFに保存」→
   プレビューのページ数が2ページ（取り組み等が無ければ1ページ）になっているか
4. **回答画面** → 試しに1件回答し、結果のレーダーチャートに
   「地域平均」が出ていないか
5. （SQLを流した場合）調査回の区分で「その他」を選んで保存できるか

---

## 6. 今回は触っていないところ

- 設問の内容・配点・集計のしかた
- 回答画面の入力の流れ
- 管理画面の集計・グラフ・CSV書き出し
- 個票（回答者カード）に出る地域平均
- 既に登録されている回答データ

以上です。ご不明な点があればお知らせください。
