# 更新パッケージ 11（bosai-update-11）

今回の修正内容と、GitHubへの上げ方をまとめました。
専門用語はできるだけ避けています。上から順にお読みください。

> このパッケージには、前回お渡しした **更新10 の内容もすべて含まれています**。
> 更新10をまだ反映していない場合でも、これだけを上げれば大丈夫です。

---

## 1. 今回直したこと（6点）

| # | ご依頼 | どう直したか |
|---|--------|--------------|
| 1 | 回答者の立場に「その他」を追加 | 回答画面・代理入力・修正画面・用紙のすべてに追加（SQLを1回実行） |
| 2 | アンケート用紙の「役員記入欄」を「担当者記入欄」に | 表記を変えました |
| 3 | 全体のデザインを紺色を基調としたものに | 緑を紺に置き換えました。差し色の橙・赤はそのまま |
| 4 | 回答者の結果用紙を携帯で画像保存できない | 保存のしくみを変え、「写真に保存する」ボタンを追加 |
| 5 | 総会用資料が、ときどき管理画面の全体印刷になってしまう | 資料を開いているあいだ、ずっと「ここだけ印刷」状態を保つようにしました |
| 6 | 結果を自治会間で比較できるようにしたい | 「自治会どうしの比較」を新設（SQLを1回実行） |

---

## 2. それぞれの中身

### 1）立場に「その他」を追加

これまでは「住民」「役員・区長」の2つだけでした。
消防団の方、民生委員の方など、どちらとも言いにくい立場の方が
答えられるよう「その他」を足しています。

追加した場所は4か所です。

- 住民の回答画面（お立場）
- 紙回答の代理入力
- 回答者ごとの個票からの修正画面
- 印刷するアンケート用紙（□3 その他）

属性別クロス集計の「立場」にも「その他」の列が出るようにしました。
**この機能は、SQLを1回実行してから有効になります**（下の「4. SQLの実行」）。

すでに登録されている回答の立場は変わりません。

### 2）「役員記入欄」→「担当者記入欄」

アンケート用紙のいちばん下の表記を変えました。
中身（□入力済／入力者／入力日）はそのままです。

### 3）紺色を基調としたデザイン

これまでの緑を、紺色に置き換えました。
差し色の橙色（注意書き・「予定」の印など）と、
警告の赤はそのまま残しています。

- 主色：**#1b3a6b**（紺）
- 濃い主色：**#12274a**（濃紺）／見出し帯・総合得点の枠など
- 淡い主色：**#e4eaf4**（淡い紺）／表の見出しなど

文字色・罫線・背景といった細かい色も、緑がかった灰色から
青みのある灰色に寄せて、全体がちぐはぐにならないようにしています。

変更にあわせて、2か所だけ色の意味を整理しました。

- **「増えた」を示す色**を緑（#0f7a5a）にしました。
  紺のままだと見出しと同じ色になり、「増えた・減った」が読み取りにくいためです
- **推移グラフの3本の線**を、濃紺・青・橙にしました。
  以前は総合と防災行動力がどちらも紺で、見分けにくくなっていました
- あわせて、取り組みの状態の「実施済」を緑にしました
  （「実施中」の青と紺で似ていたためです）

画面・アンケート用紙・総会用資料・結果画像のすべてを刷り直して確認しています。

### 4）携帯で結果の画像を保存できない件

**原因**：画像を「とても長い文字列（data:～）」として作っていました。
パソコンではこれで保存できますが、スマートフォンでは
この形式の「ダウンロード」も「写真に追加」も受け付けてもらえないことが多く、
とくに iPhone や、LINE などのアプリ内ブラウザでは、まず保存できません。

**直し方**：画像を文字列ではなく **ファイルの実体（blob）** として作るようにしました。
そのうえで、保存の道を3つ用意しています。

1. **「写真に保存する」ボタン**（スマートフォン向け）
   端末の共有画面が開き、「画像を保存」「写真に追加」を選べます。
   この方法が使える端末では、このボタンが自動的に出ます
2. **「画像をダウンロード」**（パソコン向け）
3. **「画像だけを開く」**
   画像だけの画面が開くので、長押しで保存できます。
   1と2がうまくいかないときの逃げ道です

うまくいかない場合に備えて、
「画面をそのままスクリーンショットで残しても構いません」という
一文も添えました。

### 5）総会用資料の印刷が、ときどき全体印刷になる件

**原因**：ブラウザには「利用者が押していない印刷は実行しない」という決まりがあり、
Firefox では
「このWebサイトから自動的に印刷することは禁止されています」
と出て、画面の印刷ボタンが効かないことがあります。

このとき利用者が Ctrl+P で印刷し直すわけですが、
これまでの作りでは「ここだけ印刷する」という目印を
**ボタンを押した瞬間にだけ**付けていました。
ボタンが効かなかった時点で目印は外れており、
そのあとの Ctrl+P では管理画面まるごとが刷られていた、という次第です。

**直し方**：印刷プレビューを**開いているあいだはずっと**目印を付けたままにしました。
こうしておけば、

- 画面の印刷ボタンから刷っても
- Ctrl+P（Macは ⌘+P）から刷っても
- ブラウザのメニューから刷っても

いずれの場合も、その資料だけが出ます。
目印は印刷用の指定にしか効かないので、画面の見え方は変わりません。

あわせて、プレビューの下に
「ボタンを押しても印刷画面が出ないときは、この画面を開いたまま Ctrl+P を押してください」
という案内を出すようにしました。
この修正は、アンケート用紙の印刷にも同じように効きます。

### 6）自治会どうしの比較

管理画面に「**自治会どうしの比較**」という箱が増えました
（「属性別クロス集計」のすぐ下です）。
「比較を表示する」を押すと開きます。

出るもの：

- **総合得点の並び**（横棒グラフ）
  いま表示している自治会が濃い色。点線は全体の平均です
- **一覧表**
  自治会ごとの回答数・防災行動力・初動対応力・総合得点と、
  当自治会との差
- **区分別の比較**
  避難・消火・備蓄状況など9区分を、自治会ごとに並べた表。
  いちばん高い自治会の数字が太字になります

比べるのは、各自治会の**いちばん新しい調査回**です。

**扱いについて、2点だけ配慮しています。**

- **回答が5名に満たない調査回は、集計から外しています。**
  人数が少ないと、平均から個人の回答が推測できてしまうためです
- **ご自身が管理していない自治会は、名前を伏せて**
  「他の自治会 1」「他の自治会 2」…と表示します。
  （すべての自治会をご自身で管理されている場合は、
  全部そのままの名前で出ます）

やり取りされるのは平均点だけで、個々の回答は一切含まれません。

表の下には、
「自治会によって世帯構成も地形も異なるため、順位そのものにはあまり意味がありません。
0.2点程度の差は横並びとみて差し支えありません」
という注記を入れてあります。
総会の場で順位争いになってしまわないための一文です。

**この機能も、SQLを1回実行してから使えるようになります。**

---

## 3. GitHubへの上げ方

zipを解凍すると、次のようになっています。

```
package.json
src/
  AdminDashboard.jsx        ← 差し替え
  AssocCompare.jsx          ← 新規（自治会どうしの比較）
  BosaiSurvey.jsx           ← 差し替え
  ResultImage.jsx           ← 差し替え
  ReportSheet.jsx           ← 差し替え
  SurveyForm.jsx            ← 差し替え
  QrCode.jsx                ← 差し替え
  PaperEntry.jsx            ← 差し替え
  RespondentEdit.jsx        ← 差し替え
  RespondentCards.jsx       ← 差し替え
  CrossTab.jsx              ← 差し替え
  ItemRanking.jsx           ← 差し替え
  ActionPlan.jsx            ← 差し替え
  FreeTextPanel.jsx         ← 差し替え
  lib/
    bosai-supabase-api.js   ← 差し替え
    qr.js                   ← 差し替え
sql/
  06-phase-other.sql
  07-compare-and-member-type.sql   ← 新規
```

手順は、これまでと同じです。

1. GitHubで `sugi-18/bosai-app` を開く
2. 「Add file」→「Upload files」
3. 解凍してできた **`package.json` `src` `sql` を、点線の枠の中へドラッグ＆ドロップ**
   - ※「choose your files」から選ぶ方法だと、フォルダの形が保てません。
     必ず**ドラッグ＆ドロップ**でお願いします
4. 下の「Commit changes」を押す

同じ名前のファイルは上書きされます。触っていないファイルはそのままです。
2〜3分で公開まで自動的に済みます（「Actions」タブで緑のチェックが付けば完了）。

デザインの変更でほとんどのファイルに手が入っているため、
差し替えるファイルが多くなっています。すべて上げてください。

---

## 4. SQLの実行

Supabase を開く →左の「SQL Editor」→ 中身を貼り付けて「Run」。
何度実行しても壊れません。

**`sql/07-compare-and-member-type.sql` を実行してください。**
これで「立場のその他」と「自治会どうしの比較」が使えるようになります。

まだ `sql/06-phase-other.sql`（調査回の区分に「その他」）を
実行していない場合は、**06 → 07 の順**で実行してください。

実行しないまま使おうとした場合は、画面に
「まだデータベースに登録されていません」という案内が出るようにしてあります。
壊れることはありません。

---

## 5. 確認していただきたいこと

公開後、次の順にご確認ください。

1. **画面全体**が紺色になっているか
2. **回答画面** → お立場に「その他」が出るか
3. **回答画面** → 試しに1件回答し、結果画面で「結果を画像で保存する」を押す。
   **スマートフォンで**「写真に保存する」から写真に残せるか
4. **管理画面** → 「総会用の印刷資料」を開き、
   ボタンではなく **Ctrl+P（Macは ⌘+P）** で印刷してみる。
   管理画面全体ではなく、資料だけが出るか
5. **管理画面** → 「自治会どうしの比較」→「比較を表示する」
6. **管理画面** → 「アンケート用紙の印刷」→ 一番下が「担当者記入欄」になっているか

同梱の **「見本_アンケート用紙.pdf」** で、紺色になった用紙の
刷り上がりを先にご覧いただけます。

---

## 6. 今回は触っていないところ

- 設問の内容・配点・集計のしかた
- 回答の流れ、入力のしかた
- 既に登録されている回答データ（立場の値も変わりません）

以上です。ご不明な点があればお知らせください。
