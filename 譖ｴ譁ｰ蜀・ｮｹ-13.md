# 更新パッケージ 13（bosai-update-13）

今回追加した2つの機能と、GitHubへの上げ方をまとめました。

---

## 1. 追加したこと

| # | ご依頼 | 追加した内容 |
|---|--------|--------------|
| 1 | 第一回調査に回答したか否かを選べるようにし、属性別の比較で分析できるように | 回答画面の「お立場」の上に追加。第2回以降の調査でのみ表示 |
| 2 | 回答番号を用紙に表示するかを管理画面から設定できるように | 「アンケート用紙の印刷」にチェックボックスを追加（調査回ごとに保存） |

どちらも **SQLを1回実行**してから使えるようになります。

---

## 2. 「前回の調査に回答しましたか」

### どこに出るか

回答画面の最初の画面、**「お立場」のすぐ上**に出ます。

> **2025年度 第1回のアンケートに回答されましたか**
> 　□ 選んでください　□ 回答した　□ 回答していない　□ 覚えていない

前の調査回の名称が、そのまま設問文に入ります。
「前回」と書くより、どの調査を指しているのかが伝わるためです。

### 第2回以降でのみ表示されます

**どの自治会でも、2回目以降の調査回なら自動的に表示されます。**
自治会ごとの設定は要りません。

判定は、その自治会にそれより前の調査回があるかどうかで行います。
第1回の調査では表示されないため、
回答される方に不要な問いを見せることはありません。

### 「覚えていない」を入れてあります

選択肢は「回答した」「回答していない」「覚えていない」の3つです。

1年前のことを覚えていない方に、どちらかを無理に選ばせると、
かえって集計が不正確になります。
「覚えていない」を選べるようにしておけば、
**「回答した」と答えた層の確かさが上がります。**

分析では「回答した」の層だけを見れば、
続けて答えてくださった方の変化が分かります。

不要でしたら削除できますので、お知らせください。

### 分析のしかた

管理画面の「**属性別クロス集計**」で、
「分ける軸」に「**前回への回答**」が選べるようになります。

- 「回答した」層の平均点 ＝ 継続して回答した方の現在地
- 前回の調査結果と見比べれば、その層の伸びが分かります

第1回の調査では、この軸そのものが選択肢に出ません。
全員が「未記入」の表を見せても判断の助けにならないためです。

### 入力・修正の場所も揃えてあります

- **紙回答の代理入力**：「立場」の前に同じ項目が出ます（第2回以降のみ）
- **アンケート用紙**：「回答者について」の先頭に記入欄が入ります（第2回以降のみ）
- **回答者ごとの個票**：表示され、修正画面から直せます

---

## 3. 用紙に「回答番号」欄を入れるかどうか

管理画面の「**アンケート用紙の印刷**」に、チェックボックスを追加しました。

> ☐ 用紙に「回答番号」の記入欄を入れる

- **設定は調査回ごとに保存されます。**
  番号を配る回だけチェックを入れてください
- チェックを外すと、用紙の「回答番号」欄が消え、
  「立場」の行が横いっぱいに広がります
- **既定は「入れない」です。**
  番号を配らない調査のほうが多いとうかがったためです

### ご注意

**既にある調査回も、既定では「入れない」になります。**
回答番号を使っている調査回で用紙を刷り直す場合は、
チェックを入れてから印刷してください。

なお、**回答画面（スマホ・パソコン）の回答番号欄は、これまでどおり表示されます。**
こちらは「（用紙に記載がある場合のみ）」「空欄でも回答できます」と
添えてあるため、番号を配らない調査でも迷わせにくいと考えました。
回答画面からも消したい場合はお知らせください。

---

## 4. GitHubへの上げ方

zipを解凍すると、次のようになっています。

```
src/
  BosaiSurvey.jsx        ← 差し替え（回答画面の設問）
  SurveyForm.jsx         ← 差し替え（用紙と回答番号の設定）
  PaperEntry.jsx         ← 差し替え（代理入力）
  RespondentEdit.jsx     ← 差し替え（個票からの修正）
  RespondentCards.jsx    ← 差し替え（個票の表示）
  CrossTab.jsx           ← 差し替え（属性別クロス集計の軸）
  AdminDashboard.jsx     ← 差し替え
  lib/
    bosai-supabase-api.js ← 差し替え
sql/
  09-prior-round-and-code-option.sql   ← 新規
```

1. GitHubで `sugi-18/bosai-app` を開く
2. 「Add file」→「Upload files」
3. 解凍してできた **`src` と `sql` を、点線の枠の中へドラッグ＆ドロップ**
   - ※「choose your files」から選ぶ方法だと、フォルダの形が保てません
4. 下の「Commit changes」を押す

---

## 5. SQLの実行

**`sql/09-prior-round-and-code-option.sql` を実行してください。**

Supabase を開く →左の「SQL Editor」→ 中身を貼り付けて「Run」。
何度実行しても壊れません。
06 → 07 → 08 → 09 の順に実行済みであることが前提です。

実際にPostgreSQLを動かして、次の点を確認しています。

- 2回続けて実行しても壊れないこと
- 第2回の調査回で「前回あり・前回の名称」が正しく返ること
- 第1回の調査回では「前回なし」になること
- 回答の登録で新しい項目が保存され、未選択のときは空のまま入ること
- 想定外の値（例：「たぶん」）は登録を拒むこと

### SQLを流す前でも画面は壊れません

順番が前後しても管理画面が真っ白にならないよう、
新しい項目が見つからない場合は、その項目だけ外して読み書きする作りにしてあります。
ただし新機能は動きませんので、SQLは実行してください。

---

## 6. 確認していただきたいこと

1. **回答画面**（第2回以降の調査回のURL）→
   「お立場」の上に前回への回答をうかがう欄が出るか
2. **回答画面**（第1回の調査回のURL）→ その欄が**出ない**か
3. **管理画面** → 「アンケート用紙の印刷」→ チェックを入れ／外して
   「用紙を開く」。回答番号欄が出たり消えたりするか
4. **管理画面** → 「属性別クロス集計」の「分ける軸」に
   「前回への回答」が増えているか（第2回以降の調査回で）

用紙は、4通り（第1回／第2回 × 回答番号あり／なし）すべてを
実際にA4へ出力し、2枚に収まることを確認しています。

---

## 7. 今回は触っていないところ

- 設問の内容・配点・集計のしかた
- 画面のデザイン、印刷まわり
- 既に登録されている回答データ

以上です。ご不明な点があればお知らせください。
